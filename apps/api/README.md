将http错误分为两类:
- 第一类：无需前端特殊处理的，只需要提示告知用户即可
- 第二类：需要前端辨别并进行处理的，比如余额不足，需要引导用户充值


对于第一类可以在service中直接抛出错误，前端捕获错误做统一处理，例如弹出toast 即可
对于第二类错误不要在service中抛出错误，而应该在controller中返回错误，这样错误的类型可以被openapi或elysia的类型系统感知传递给前端，更方便与前端联调
